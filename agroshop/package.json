{
  "name": "agroshop",
  "version": "1.0.0",
  "private": true,
  "main": "electron/main.js",
  "description": "Offline fertilizer & pesticides shop management — React + Vite, Express, SQLite. Runs on one PC.",
  "scripts": {
    "setup": "npm install && npm --prefix backend install && npm --prefix frontend install && npm run seed",
    "electron:rebuild": "electron-rebuild -f -w better-sqlite3 --module-dir backend",
    "rebuild:node": "npm rebuild better-sqlite3 --prefix backend",
    "seed": "npm --prefix backend run seed",
    "seed:reset": "npm --prefix backend run seed -- --reset",
    "dev": "concurrently -n api,web -c green,cyan \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "start": "concurrently -n api,web -c green,cyan \"npm --prefix backend start\" \"npm --prefix frontend run dev\"",
    "test": "npm --prefix backend test",
    "build": "npm --prefix frontend run build",
    "electron:build": "npm --prefix frontend run build && npm run electron:rebuild && electron-builder --win nsis"
  },
  "build": {
    "appId": "com.agroshop.desktop",
    "productName": "AgroShop",
    "directories": {
      "output": "dist-electron"
    },
    "files": [
      "electron/**/*",
      "backend/src/**/*",
      "backend/package.json",
      "backend/node_modules/**/*",
      "frontend/dist/**/*",
      "package.json"
    ],
    "asarUnpack": [
      "node_modules/better-sqlite3/**/*",
      "backend/node_modules/better-sqlite3/**/*"
    ],
    "win": {
      "target": "nsis"
    },
    "nsis": {
      "oneClick": false,
      "perMachine": false,
      "allowToChangeInstallationDirectory": true
    }
  },
  "devDependencies": {
    "@electron/rebuild": "^3.7.1",
    "concurrently": "^9.0.1",
    "electron": "^32.2.7",
    "electron-builder": "^25.1.8"
  }
}
